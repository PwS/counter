# counter_reactive: RxDart

The Flutter counter built with an RxDart `BehaviorSubject` and a `StreamBuilder`, with no
BLoC library.

```bash
flutter pub get
flutter run
```

See the [main README](../README.md#3-counter_reactive-rxdart) for the full walkthrough.
